# Development overrides — automatically loaded by `docker compose up`
# Remove or rename this file for production deployments.
services:
  backend:
    volumes:
      - ./backend/app:/app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
